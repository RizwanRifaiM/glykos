services:
  - type: web
    name: glykos-chatbot
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEY
        sync: false
